# Environment variables for the backend server

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=petservice
DB_USER=petuser
DB_PASSWORD=petpass

# Server configuration
PORT=3001

# Admin token for protected routes (change this in production!)
ADMIN_TOKEN=changeme
